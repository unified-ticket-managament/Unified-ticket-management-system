# v1.1 Enhancements

No formally-scheduled "v1.1" milestone exists in the repository. This document lists near-term, low-effort improvements directly implied by gaps this documentation pass found — reasonable "next" candidates, not commitments.

| Item | Why it's a reasonable near-term candidate | Related |
|---|---|---|
| Add `asyncio_default_fixture_loop_scope` (or a per-test engine) to fix the 3-file pytest hang | Small, well-understood fix; unblocks running the full backend test suite in one invocation | [11-testing/integration-testing.md](../11-testing/integration-testing.md) |
| Reconcile the three different "supervisor role" constants across frontend/backend | Confirmed drift, no functional bug yet, but a latent one | [05-technical-architecture/frontend-architecture.md](../05-technical-architecture/frontend-architecture.md) |
| Fix `change_password`'s latent transient-object `db.refresh()` bug | Identical, already-diagnosed shape to the `update_profile` bug that *was* fixed | [14-troubleshooting/authentication/README.md](../14-troubleshooting/authentication/README.md) |
| Live-verify business-critical notification email against a real SMTP server | Currently unit-tested only, explicitly flagged as not yet production-proven | [16-known-limitations/integration-limitations.md](../16-known-limitations/integration-limitations.md) |
| Clean up the two leftover `ACTIVE` escalation rows (or scope the test's own assertion) causing a known pytest flake | Small, well-understood, just not yet done | [11-testing/integration-testing.md](../11-testing/integration-testing.md) |
| Resolve whether `render.yaml` or the EC2/systemd path is authoritative production, and update `DEPLOYMENT.md` accordingly | A real operational ambiguity this documentation pass could not resolve from the repository alone | [09-deployment/environments.md](../09-deployment/environments.md) |
| Reconcile the port mismatch between the EC2 health check (8001) and Render/`start.sh`'s default (8000) | Small, concrete, currently just "worth knowing" rather than fixed | [09-deployment/health-checks.md](../09-deployment/health-checks.md) |

None of the above are tracked in a formal issue tracker visible to this documentation pass — treat this list as a starting point for triage, not a committed plan.
