// Active Staff users in the RBAC `users` table — kept in sync
// manually until there's a `GET /users?role=Staff` endpoint to
// fetch this list instead.
export const AGENTS = ["Emma Watts", "John Carter", "Liam Brooks"];

export const DEFAULT_AGENT = AGENTS[0];
