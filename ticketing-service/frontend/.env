# rbac-service/backend and ticketing-service/backend were merged into
# one unified backend (unified-backend/) — both now live on the same
# port/process, so this and VITE_RBAC_API_BASE_URL below point at the
# same host.
VITE_API_BASE_URL=http://localhost:8000
#VITE_API_BASE_URL=https://ticket-management-backend-skct.onrender.com

VITE_RBAC_API_BASE_URL=http://localhost:8000/api/v1

VITE_RBAC_FRONTEND_URL=http://localhost:3000
