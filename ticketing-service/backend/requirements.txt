fastapi==0.138.0
uvicorn==0.49.0
starlette==1.3.1

sqlalchemy==2.0.51
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.18.4

pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
python-multipart==0.0.32
python-dotenv==1.2.2

python-jose==3.3.0
cryptography==49.0.0

httpx==0.28.1
boto3==1.43.39

# Local editable install — shared_models lives in this monorepo as the
# single source of truth (see ../../shared_models and rbac-service's
# own requirements.txt, which uses the same pattern).
-e ../../shared_models
